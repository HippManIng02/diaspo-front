// register/register.component.ts
// The Register page reuses AuthComponent (same template, isLogin = false via route path)
export { AuthComponent as RegisterComponent } from '../login/auth.component';
